你的任务是为本次会话创建一份完整的压缩总结。这份总结将被置于新会话的开头，作为持续工作的上下文；后续围绕此上下文的新消息会跟随其后（你现在看不到那些内容）。请尽可能详尽地总结，使得只阅读此摘要和新消息的人，也能完整理解已发生的内容并继续推进工作。

在输出最终总结之前，请用 <analysis> 标签包裹你的分析过程，确保覆盖所有必要要点。分析步骤如下：

1. 按时间顺序逐条分析对话中的每条消息和每个阶段，对每个阶段详细识别：
   - 用户的明确请求与意图
   - 你处理请求的方式和策略
   - 关键决策、新闻主题判断、检索关键词选择
   - 具体细节，例如：
     - 检索的关键词与查询语句
     - 访问过的信息源 URL 与摘要
     - 已生成的摘要或推送内容全文
     - 用户对输出的反馈与调整
   - 遇到的错误（如检索结果偏差、摘要失实）及修正方式
   - 用户明确要求改变处理方式的反馈，须特别标注

2. 核查技术准确性与完整性，确保每个必要要素都得到充分覆盖。

---

你的总结须包含以下各节，每节说明填写要求：

会话标题：一句话概括本次会话的核心任务。

1. 当前任务与用户意图：详细描述用户本次的核心请求——追踪哪些话题、关注哪些领域、期望何种输出形式（摘要/推送/报告/问答）、有无时效或字数要求。若用户意图在会话中途有调整，须同时记录初始意图与最终意图。

2. 关键新闻主题与技术概念：列出本次会话涉及的核心新闻话题。目的是让下一会话无需重读对话即可快速定位语境，每条须足够具体。

3. 已检索的查询：逐条列出本次会话使用过的全部检索关键词或查询语句，包含结果概述。此表的核心用途是去重。若某次检索因结果不佳而重新调整了关键词，两次均须记录并注明关系。

4. 已访问的信息源：记录本次会话中实际读取过内容的所有来源，包含 URL 或文件名、内容摘要（2句以内）、可信度评估。摘要须反映对本次任务有用的关键信息点，不得泛写「介绍了相关内容」。

5. 已生成的输出成果：逐项列出本次会话向用户实际交付的输出，包括摘要、推送文案、分析报告、问答回复等。每项须记录输出类型、交付时间及正文全文或完整核心内容，不得仅写「生成了摘要」。

6. 错误与修正记录：记录本次会话中出现的检索偏差、摘要失实、话题遗漏、用户纠错等问题，逐条说明问题描述、原因分析与修正方式。若本次会话无错误，填「无」。

7. 所有用户消息：按顺序列出本次会话中用户发送的所有非工具调用消息，原文完整保留，不得缩写、合并或省略。若用户有明确的偏好调整或纠错指令，在该条消息后加注「 偏好变更」标记。

8. 待跟进线索：列出用户提及但本次会话尚未完成检索或分析的话题、需持续追踪的滚动事件、用户隐含但未明说的潜在关注点。每条须说明「为何值得跟进」，不得泛写「还有很多话题未覆盖」。

9. 会话工作日志：按时间顺序逐步记录本次会话的完整操作，包括用户请求、执行检索、访问信息源、生成内容、用户反馈、内容修正等每一个动作节点。时间格式统一为 `HH:MM`，每条记录须包含操作类型与操作内容摘要。

10. 继续工作所需的关键上下文：总结继续本次工作所需的全部背景信息，包含：用户确认的输出风格偏好、用户明确排除的内容或话题、当前工作进展状态、以及建议的下一步具体动作。确保下一会话读完此节后无需重新协商即可直接推进。

---

输出示例结构（仅展示格式骨架，字段含义见上方说明）：

<example>
<analysis>
[按时间顺序逐条分析，覆盖检索查询、信息源、生成内容、用户反馈、错误修正等全部要点]
</analysis>

<summary>

## 会话标题
[一句话标题]

## 1. 当前任务与用户意图
[任务描述]

## 2. 关键新闻主题
- [具体话题1]
- [具体话题2]

## 3. 已检索的查询
| 查询关键词  | 结果概述 |
|-----------|---------|
| [关键词]  |  |

## 4. 已访问的信息源
| 来源类型 | URL / 名称 | 内容摘要 | 可信度评估 |
|---------|-----------|---------|-----------|
| [互联网/本地] | |  |  |

## 5. 已生成的输出成果
- [输出类型（时间）]：[正文全文或完整核心内容]

## 6. 错误与修正记录
| 问题描述 | 原因分析 | 修正方式 |
|---------|---------|---------|
| [问题] | [原因] | [修正] |

## 7. 所有用户消息
- 「[原文]」
- 「[原文]」 偏好变更

## 8. 待跟进线索
- [线索]（原因：[为何值得跟进]）

## 9. 会话工作日志
- `HH:MM` [操作类型]：[内容摘要]

## 10. 继续工作所需的关键上下文
- 输出风格偏好：[具体描述]
- 排除内容：[具体描述或「无」]
- 当前进展：[状态描述]
- 下一步动作：[具体可执行的操作]

</summary>
</example>

请严格按照上述结构输出总结，确保准确、详尽，特别是检索记录、信息源溯源和用户消息须完整保留，不得省略。